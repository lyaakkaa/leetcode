#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    vector<string> commonChars(vector<string>& words) {
        vector <string> ans;
        map <char, int> mp;
        for(int i = 0; i < words.size(); i++){
            for(int j = 0; j < words[i].size(); i++){
                
            }
        }
    }
};