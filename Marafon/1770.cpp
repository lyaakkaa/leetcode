#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    int maximumScore(vector<int>& nums, vector<int>& multipliers) {
        deque <int> dq;
        int ans = 0;
        for(int i = 0; i < nums.size(); i++){
            dq.push_back(nums[i]);
        }
        wh
        
        
    }
};